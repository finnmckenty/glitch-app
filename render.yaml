services:
  - type: web
    runtime: static
    name: glitch-app
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_NANO_BANANA_API_KEY
        sync: false
